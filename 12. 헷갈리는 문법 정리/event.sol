//event 함수

// SPDX-License-Identifier: GPL-3.0
pragma solidity >=0.7.0 < 0.9.0;

//어떤일이 발생했을때, 컨트랙트에서 다른 컨트랙트 또는 디앱과같은 클라이언트에 알려주는 역할을 함 
//실시간 알림 / 장기적인 로깅 (로그에 기록됨 / 이벤트 선언시 정의한 키로 색인화되어 분류_ 최대 3개입력 매개변수)